import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => ({
  buttonText: {
    color: 'black',
    fontFamily: 'Sora',
    marginLeft: '1em',
    fontSize: '0.8em',
    marginTop: '-1.5em',
  },
}))

export default useStyles
